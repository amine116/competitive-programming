#include<bits/stdc++.h>
#define ll long long
using namespace std;
int main(){
    ll sum = 0LL, n; scanf("%I64d", &n);
    for(ll i = 0; i < n; i++){ll temp; scanf("%I64d", &temp); sum += temp;}
    ll sum2 = 0LL;
    for(ll i = 0; i < n; i++){ll temp; scanf("%I64d", &temp); sum2 += temp;}
    if(sum >= sum2)printf("Yes\n");
    else printf("No\n");
    return 0;
}
